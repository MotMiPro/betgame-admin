import SwapEdited from "./SwapEdited";
import React, { Fragment } from "react";

function SwapConfig() {
  return (
    <Fragment>
      <SwapEdited />
    </Fragment>
  );
}

export default React.memo(SwapConfig);
